# LLM Fraud Explanation Layer Prototype

This report was generated by `04_llm_fraud_explanation_layer.ipynb`.

## Purpose

This offline prototype converts fraud model outputs and SHAP feature contributions into human-readable investigation explanations.
No OpenAI API, LangChain call, or paid API is used in this notebook.

## Model Context

- Final model: Random Forest
- Feature version: Version B - Without ExtremeFlags
- Approximate performance: PR-AUC 0.816, ROC-AUC 0.986, precision 0.924, recall 0.768, F1 0.839
- Decision threshold used in examples: 0.50

## Grounding Rules

- Explanations are grounded in model outputs and SHAP feature contributions.
- V1-V28 are anonymized PCA transaction behavior components.
- The explanation layer does not claim to know the real business meaning of anonymized PCA features.
- Generated text should support analyst review, not replace human investigation judgment.

## Example Explanations

### TXN_HIGH_RISK_001 - high-risk fraud prediction

**Prediction:** Fraud
**Fraud probability:** 0.962

**Risk summary:** Transaction TXN_HIGH_RISK_001 was classified as Fraud with a fraud probability of 0.962, which is above the 0.50 model decision threshold. This places the case in a very high risk band. The available amount fields show transaction_amount=245.31 and log_amount=5.507.

**Key model drivers:** Positive SHAP drivers that increased the fraud score: V14, an anonymized PCA transaction behavior component (V14, SHAP +0.0955); V12, an anonymized PCA transaction behavior component (V12, SHAP +0.0457); V4, an anonymized PCA transaction behavior component (V4, SHAP +0.0237); V3, an anonymized PCA transaction behavior component (V3, SHAP +0.0160). Negative SHAP drivers that reduced the fraud score: V10, an anonymized PCA transaction behavior component (V10, SHAP -0.0211); V11, an anonymized PCA transaction behavior component (V11, SHAP -0.0089); V17, an anonymized PCA transaction behavior component (V17, SHAP -0.0042); V16, an anonymized PCA transaction behavior component (V16, SHAP -0.0021).

**Suggested investigation action:** Prioritize manual review, verify transaction context, and consider temporary hold or step-up authentication according to policy.

**Confidence caveat:** This explanation is grounded in model outputs and SHAP feature contributions. Features V1-V28 are anonymized PCA transaction behavior components, so the system does not assign them specific business meanings. The explanation should support analyst review rather than replace investigation judgment.

### TXN_HIGH_RISK_002 - high-risk fraud prediction

**Prediction:** Fraud
**Fraud probability:** 0.887

**Risk summary:** Transaction TXN_HIGH_RISK_002 was classified as Fraud with a fraud probability of 0.887, which is above the 0.50 model decision threshold. This places the case in a very high risk band. The available amount fields show transaction_amount=82.0 and log_amount=4.419.

**Key model drivers:** Positive SHAP drivers that increased the fraud score: V12, an anonymized PCA transaction behavior component (V12, SHAP +0.0800); V4, an anonymized PCA transaction behavior component (V4, SHAP +0.0311); V3, an anonymized PCA transaction behavior component (V3, SHAP +0.0186); V10, an anonymized PCA transaction behavior component (V10, SHAP +0.0123). Negative SHAP drivers that reduced the fraud score: V11, an anonymized PCA transaction behavior component (V11, SHAP -0.0138); V17, an anonymized PCA transaction behavior component (V17, SHAP -0.0049); V16, an anonymized PCA transaction behavior component (V16, SHAP -0.0022).

**Suggested investigation action:** Prioritize manual review, verify transaction context, and consider temporary hold or step-up authentication according to policy.

**Confidence caveat:** This explanation is grounded in model outputs and SHAP feature contributions. Features V1-V28 are anonymized PCA transaction behavior components, so the system does not assign them specific business meanings. The explanation should support analyst review rather than replace investigation judgment.

### TXN_FALSE_NEGATIVE_001 - false negative

**Prediction:** Legitimate
**Fraud probability:** 0.431

**Risk summary:** Transaction TXN_FALSE_NEGATIVE_001 was classified as Legitimate with a fraud probability of 0.431, which is below the 0.50 model decision threshold. This places the case in a near-threshold risk band. The available amount fields show transaction_amount=19.99 and log_amount=3.044.

**Key model drivers:** Positive SHAP drivers that increased the fraud score: V14, an anonymized PCA transaction behavior component (V14, SHAP +0.0438); V4, an anonymized PCA transaction behavior component (V4, SHAP +0.0163). Negative SHAP drivers that reduced the fraud score: V3, an anonymized PCA transaction behavior component (V3, SHAP -0.0506); V10, an anonymized PCA transaction behavior component (V10, SHAP -0.0223); V11, an anonymized PCA transaction behavior component (V11, SHAP -0.0125); V17, an anonymized PCA transaction behavior component (V17, SHAP -0.0066).

**Suggested investigation action:** Review as a missed fraud example; consider whether threshold tuning, new features, or secondary rules could improve capture.

**Confidence caveat:** This explanation is grounded in model outputs and SHAP feature contributions. Features V1-V28 are anonymized PCA transaction behavior components, so the system does not assign them specific business meanings. The explanation should support analyst review rather than replace investigation judgment.

### TXN_FALSE_NEGATIVE_002 - false negative

**Prediction:** Legitimate
**Fraud probability:** 0.277

**Risk summary:** Transaction TXN_FALSE_NEGATIVE_002 was classified as Legitimate with a fraud probability of 0.277, which is below the 0.50 model decision threshold. This places the case in a low-to-moderate risk band. The available amount fields show transaction_amount=0.0 and log_amount=0.0.

**Key model drivers:** Positive SHAP drivers that increased the fraud score: V12, an anonymized PCA transaction behavior component (V12, SHAP +0.0267); V11, an anonymized PCA transaction behavior component (V11, SHAP +0.0069). Negative SHAP drivers that reduced the fraud score: V14, an anonymized PCA transaction behavior component (V14, SHAP -0.0676); V3, an anonymized PCA transaction behavior component (V3, SHAP -0.0226); V10, an anonymized PCA transaction behavior component (V10, SHAP -0.0133); V16, an anonymized PCA transaction behavior component (V16, SHAP -0.0040).

**Suggested investigation action:** Review as a missed fraud example; consider whether threshold tuning, new features, or secondary rules could improve capture.

**Confidence caveat:** This explanation is grounded in model outputs and SHAP feature contributions. Features V1-V28 are anonymized PCA transaction behavior components, so the system does not assign them specific business meanings. The explanation should support analyst review rather than replace investigation judgment.

### TXN_LOW_RISK_001 - legitimate low-risk transaction

**Prediction:** Legitimate
**Fraud probability:** 0.018

**Risk summary:** Transaction TXN_LOW_RISK_001 was classified as Legitimate with a fraud probability of 0.018, which is below the 0.50 model decision threshold. This places the case in a low risk band. The available amount fields show transaction_amount=32.45 and log_amount=3.509.

**Key model drivers:** Positive SHAP drivers that increased the fraud score: V17, an anonymized PCA transaction behavior component (V17, SHAP +0.0028). Negative SHAP drivers that reduced the fraud score: V14, an anonymized PCA transaction behavior component (V14, SHAP -0.0875); V12, an anonymized PCA transaction behavior component (V12, SHAP -0.0419); V4, an anonymized PCA transaction behavior component (V4, SHAP -0.0217); V3, an anonymized PCA transaction behavior component (V3, SHAP -0.0146).

**Suggested investigation action:** No immediate fraud action is suggested by the model; continue routine monitoring unless external risk signals appear.

**Confidence caveat:** This explanation is grounded in model outputs and SHAP feature contributions. Features V1-V28 are anonymized PCA transaction behavior components, so the system does not assign them specific business meanings. The explanation should support analyst review rather than replace investigation judgment.

### TXN_LOW_RISK_002 - legitimate low-risk transaction

**Prediction:** Legitimate
**Fraud probability:** 0.006

**Risk summary:** Transaction TXN_LOW_RISK_002 was classified as Legitimate with a fraud probability of 0.006, which is below the 0.50 model decision threshold. This places the case in a low risk band. The available amount fields show transaction_amount=11.2 and log_amount=2.501.

**Key model drivers:** Positive SHAP drivers that increased the fraud score: No material positive SHAP drivers were provided for this example. Negative SHAP drivers that reduced the fraud score: V12, an anonymized PCA transaction behavior component (V12, SHAP -0.0762); V4, an anonymized PCA transaction behavior component (V4, SHAP -0.0296); V10, an anonymized PCA transaction behavior component (V10, SHAP -0.0156); V11, an anonymized PCA transaction behavior component (V11, SHAP -0.0099).

**Suggested investigation action:** No immediate fraud action is suggested by the model; continue routine monitoring unless external risk signals appear.

**Confidence caveat:** This explanation is grounded in model outputs and SHAP feature contributions. Features V1-V28 are anonymized PCA transaction behavior components, so the system does not assign them specific business meanings. The explanation should support analyst review rather than replace investigation judgment.

## Future Integration Path

This offline prototype can later connect to the OpenAI API, LangChain, or a Streamlit app by replacing the rule-based generator with a controlled LLM call.
The same structured prompt and grounding rules should be preserved so the LLM converts SHAP outputs into readable explanations without inventing unsupported reasons.